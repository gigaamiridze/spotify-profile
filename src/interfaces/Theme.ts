export interface ITheme {
  colors: IColors;
  fonts: IFonts;
}

interface IColors {
  white: string;
  black: string;
  eerieBlack: string;
  green: string;
}

interface IFonts {
  primary: string;
}