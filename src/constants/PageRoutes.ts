export enum PageRoutes {
  HOST = 'http://localhost:5173',
  ROOT = '/',
  ARTISTS = 'artists',
  ARTIST = 'artist',
  TRACKS = 'tracks',
  TRACK = 'track',
  RECENT = 'recent',
  PLAYLISTS = 'playlists',
}
