export enum PageRoutes {
  HOST = 'http://localhost:5000',
  ROOT = '/',
  ARTISTS = 'artists',
  ARTIST = 'artist',
  TRACKS = 'tracks',
  TRACK = 'track',
  RECENT = 'recent',
  PLAYLIST = 'playlist',
  PLAYLISTS = 'playlists',
  RECOMMENDATIONS = 'recommendations',
}
